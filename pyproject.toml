[project]
name = "mirror"
version = "0.1.0"
description = "MIRROR evidence-bundle reference tooling"
requires-python = ">=3.12"
dependencies = [
  "jsonschema==4.23.0",
  "pytest==8.3.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
